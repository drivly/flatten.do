{
  "name": "flatten.do",
  "version": "1.0.0",
  "description": "Flatten JSON Objects",
  "repository": "git@github.com:drivly/flatten.do.git",
  "license": "MIT",
  "module": "worker.js",
  "scripts": {
    "dev": "wrangler dev worker.js",
    "deploy": "wrangler publish worker.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "dependencies": {
    "flat": "^5.0.2"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "wrangler": "^2.0.27"
  }
}
